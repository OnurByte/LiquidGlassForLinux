pub const PROMPT_VERSION: u32 = 4;

pub const SVG_PROMPT: &str = "Reconstruct the attached application icon as one clean 1024 by 1024 SVG. Preserve its exact recognizable identity, silhouette, proportions, logo geometry, typography converted to paths, and meaningful details. Use a full-canvas opaque <g id=\"background\"> as the canvas background, followed by one to four foreground groups named <g id=\"foreground-1\"> through <g id=\"foreground-4\"> in back-to-front z order. Split visually independent parts, colors, and depth-bearing shapes into separate groups when that improves material separation, but never invent empty groups or duplicate artwork. Return two to five top-level artwork groups total. Keep the artwork flat and vector-only. Do not embed raster images, external resources, scripts, text/font elements, filters, blur, shadows, glow, bevel, transparency effects, masks, rounded-square canvas masks, mockups, UI screenshots, or invented symbols. Return only the JSON object required by the output schema.";
